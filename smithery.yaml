# Smithery deployment config for mcp-skill-sec
# See https://smithery.ai/docs/build for the schema.
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |
    (config) => ({ command: 'python3', args: ['/app/mcp_server.py'] })
